#include "player/player.hpp"

int main()
{
  // Player player('j');
  // player.getInfo();
  // int maxLoops = 600;
  // while (maxLoops)
  //{
  //   auto [currentX, currentY] = player.getPosition();
  //   player.move(--currentX, ++currentY);
  //   player.getInfo();
  //   --maxLoops;
  // }
  return 0;
}
